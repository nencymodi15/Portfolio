const express = require("express");
const mongoose = require("mongoose");
const app = express();
const exphbs = require("express-handlebars");
const bodyParser = require("body-parser");   


app.listen(PORT, ()=>{ console.log("go to http http://localhost:3000/")

})